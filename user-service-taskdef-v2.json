{
  "family": "user-service",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "512",
  "memory": "1024",
  "executionRoleArn": "arn:aws:iam::748189524661:role/ecsTaskExecutionRole-my-app",
  "taskRoleArn": "arn:aws:iam::748189524661:role/ecsTaskRole-my-app",
  "tags": [
    {"key": "Project", "value": "my-app"},
    {"key": "Env", "value": "prod"}
  ],
  "containerDefinitions": [{
    "name": "user-service",
    "image": "748189524661.dkr.ecr.ap-south-1.amazonaws.com/my-app/user-service:latest",
    "essential": true,
    "portMappings": [{
      "containerPort": 8081,
      "protocol": "tcp"
    }],
    "environment": [
      {"name": "SERVER_PORT", "value": "8081"}
    ],
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "/ecs/user-service",
        "awslogs-region": "ap-south-1",
        "awslogs-stream-prefix": "ecs"
      }
    },
    "healthCheck": {
      "command": ["CMD-SHELL", "curl -f http://localhost:8081/actuator/health || exit 1"],
      "interval": 30,
      "timeout": 5,
      "retries": 3,
      "startPeriod": 60
    }
  }]
}
